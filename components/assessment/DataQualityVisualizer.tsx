import React from 'react';

const DataQualityVisualizer: React.FC = () => {
    // This component is deprecated and should not be used.
    // Returning null to ensure it renders nothing if accidentally included.
    return null; 
};

export default DataQualityVisualizer;